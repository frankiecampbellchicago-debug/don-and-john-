# Images

These 5 photos are pulled from the "Don & John Redesign — Home with Wipe Effect"
Stitch reference and downloaded locally so the site doesn't depend on Google's
CDN:

| Filename                  | Used for                                    |
|----------------------------|----------------------------------------------|
| `hero-bg.jpg`              | Hero background, behind the squeegee-wipe effect |
| `gallery-windows.jpg`      | Gallery — window cleaning                    |
| `gallery-gutters.jpg`      | Gallery — gutter cleaning                    |
| `gallery-driveway.jpg`     | Gallery — driveway cleaning                  |
| `gallery-housewash.jpg`    | Gallery — house washing                      |

`hero-bg.jpg` is capped at 768x512 (the source's max native resolution), so
it's a little soft when stretched full-bleed — the dark overlay + dust/blur
"dirty layer" mostly hides this. Swap in a higher-res photo of the same shot
any time by replacing the file (same filename, same aspect ratio ideally).
